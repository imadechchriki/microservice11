{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Services": {
    "QuestionnaireService": "${QUESTIONNAIRE_SERVICE_URL}",
    "AuthService": "${AUTH_SERVICE_URL}",
    "CatalogService": "${CATALOG_SERVICE_URL}"
  }
}